1. 后端使用DDD架构，Jdk17,SpringBoot2.7.14,JPA,hibernate,h2sql,lombok,mapstruct,Axon Framework,SpringEvent
2. 前端使用 React 18 + Vite + Ant Design + Axios，函数式组件风格，偏好简洁、职责明确的函数；变量、组件命名清晰；消除 ESLint/TS 警告。
3. 所有命名均遵循语义清晰、风格统一（PascalCase / camelCase）。
4. 无论是 TypeScript、Java，还是 ESLint，都希望 zero-warning 状态，辅助提升代码健康度。
5. 注释使用中文，采用 Javadoc 风格；关键模块和函数必须有注释。
6. 强调单元和集成测试覆盖率（后端使用 JUnit，前端 Jest/RTL）。