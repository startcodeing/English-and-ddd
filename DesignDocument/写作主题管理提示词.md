我需要在现有系统中新增一个“写作主题管理”模块，属于内容管理模块下的子模块。该模块主要用于管理“写作练习”使用的题目信息，功能包括：列表查询、新增、编辑、删除。

请使用以下技术生成完整的前后端代码，并完成前后端联调：

- 后端使用：Spring Boot + JPA (可以参考当前已有代码的风格)
- 前端使用：React（配合 Ant Design）
- 数据模型保存在 domain-content 模块中，控制层和服务实现放在 content 相关的模块中
- 前后端通过 RESTful 接口交互

该模块的数据库结构如下：

- 表名：`writing_topic`
- 字段：
    - id: Long
    - description: String（主题描述）
    - source: String（题目来源）
    - difficulty: Enum（easy, medium, hard）
    - word_limit: Integer（字数限制）
    - time_limit: Integer（单位：分钟，可选）
    - create_time: datetime
    - update_time: datetime

具体要求如下：

1. 后端：
    - 使用 JPA 实体类定义字段映射，添加合理的字段注解
    - 编写domain，application,infrastructure,interfaces层所有相关的实现
    - 提供分页查询、条件搜索（如根据 difficulty / source 查询）的接口
    - 提供新增、编辑、删除接口
    - 返回统一响应结构（如 Result<T>）

2. 前端：
    - 使用 React 编写页面，组件库可以参考当前已有功能的实现
    - 提供：
        - 列表页（含分页、搜索、编辑、删除）
        - 表单页（用于新增和编辑）
    - 与后端进行完整联调，调用实际接口

3. 其它说明：
    - 表单中 difficulty 使用下拉选择框
    - create_time 和 update_time 后端自动填充
    - 联调后确保完整可用（前端能发起请求并接收到正确响应）